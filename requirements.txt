# meridian — pinned dependencies
# Python 3.11.9
# Install: pip install -r requirements.txt

# Core inference (required for run.sh / scoring pipeline)
pandas==2.3.3
numpy==2.4.1
lightgbm==4.6.0
scipy==1.17.0
scikit-learn==1.5.2
pyarrow==22.0.0
python-dotenv==1.2.2

# Dashboard (required for streamlit_app/)
streamlit>=1.35.0
plotly==6.5.2

# AI Copilot (optional — enables Groq LLM; rule-based fallback works without it)
groq>=0.9.0

# Testing
pytest==8.4.2
pytest-cov>=4.1.0
